{
  "_comment": "EC2 / Linux VM / WSL Remote — same as cursor-mcp.template.json. Generate: npm run cursor-mcp:config -- --variant linux",
  "mcpServers": {
    "techsapo-providers": {
      "command": "<NODE_EXECUTABLE>",
      "args": ["<REPO_ROOT>/dist/services/techsapo-providers-mcp-server.js"],
      "cwd": "<REPO_ROOT>"
    }
  }
}
